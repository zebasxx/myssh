# MySSH

A small static SSH connection manager. It stores folders and connection settings in browser `localStorage`.

## Run

Open `index.html` in a browser.

## Features

- Folder tree for organizing SSH connections
- Connection editor with name, DNS/IP, port, username, and private key path
- SSH command preview with copy action
- Duplicate and delete actions
- Search across names, hosts, usernames, and key paths
